# Binario y artefactos de build
easyzfs
easyzfs-local
bin/
*.test

# Datos en runtime
*.db
*.db-wal
*.db-shm

# Front (web/ se genera con el stack PWA; dist/ se construye por make)
web/node_modules/
web/dist/
web/*.tsbuildinfo
dist/

# Go
/vendor/

# Zips de trabajo (no pertenecen al repo)
*.zip

# Credenciales locales (NUNCA subir)
easyzfs_credenciales.txt
